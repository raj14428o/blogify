<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('./partials/head') %>
  <title>Create Blog</title>

  <style>
    body {
      background: #e5e7eb;
      margin: 0;
    }

    .editor-wrapper {
      min-height: calc(100vh - 60px);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem 1rem;
    }

    .editor-card {
      width: 100%;
      max-width: 720px;
      background: #ffffff;
      border-radius: 16px;
      padding: 2.2rem;
      box-shadow:
        0 20px 40px rgba(0,0,0,0.18),
        0 6px 16px rgba(0,0,0,0.12);
    }

    .editor-title {
      font-size: 1.6rem;
      font-weight: 800;
      margin-bottom: 1.8rem;
      color: #111827;
      text-align: center;
    }

    label {
      font-weight: 600;
      font-size: 0.9rem;
      margin-bottom: 6px;
      color: #111827;
    }

    input, textarea {
      width: 100%;
      border: 1.6px solid #d1d5db;
      border-radius: 10px;
      padding: 12px 14px;
      font-size: 0.95rem;
      transition: border 0.2s, box-shadow 0.2s;
    }

    input:focus,
    textarea:focus {
      outline: none;
      border-color: #111111;
      box-shadow: 0 0 0 3px rgba(0,0,0,0.15);
    }

    textarea {
      min-height: 180px;
      resize: vertical;
    }

    .form-text {
      font-size: 0.75rem;
      color: #6b7280;
      margin-top: 4px;
    }

    .submit-btn {
      width: 100%;
      margin-top: 1.6rem;
      padding: 13px;
      font-weight: 600;
      font-size: 1rem;
      border-radius: 12px;
      border: none;
      color: white;
      background: linear-gradient(135deg, #111111, #000000);
      cursor: pointer;
      transition: transform 0.15s ease, box-shadow 0.15s ease;
    }

    .submit-btn:hover {
      transform: translateY(-1px);
      box-shadow: 0 10px 25px rgba(0,0,0,0.35);
    }
  </style>
</head>

<body>
  <%- include('./partials/nav') %>

  <div class="editor-wrapper">
    <div class="editor-card">

      <div class="editor-title">Create a new blog</div>

      <form action="/blog" method="post" enctype="multipart/form-data">

        <div class="mb-3">
          <label>Cover Image</label>
          <input type="file" name="coverImage" required>
          <div class="form-text">
            Recommended size: 1200 × 630
          </div>
        </div>

        <div class="mb-3">
          <label>Title</label>
          <input type="text" name="title" placeholder="Enter blog title" required>
        </div>

        <div class="mb-3">
          <label>Description</label>
          <input type="text" name="description" placeholder="Short summary (shown on cards)">
        </div>

        <div class="mb-3">
          <label>Body</label>
          <textarea name="body" placeholder="Write your blog content here..." required></textarea>
        </div>

        <button type="submit" class="submit-btn">
          Publish Blog
        </button>

      </form>

    </div>
  </div>

  <%- include('./partials/scripts') %>
   <%- include("./partials/socket") %>
</body>
</html>
